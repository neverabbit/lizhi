<!-- <%= form_for(@position, url: {action: 'offline', id: @position.id}, method: :post, html: {id: "position_switch" }, remote: true) do |f| %>
	<%= f.submit "下线", class: "btn small" %>
<% end %> -->

<%= form_tag({controller: 'positions', action: 'offline'}, {id: "position_switch"}) do %>
	
	<%= hidden_field_tag :id, @position.id %>
	<%= submit_tag "下线", class: 'submit small' %>
<% end %>
